========================================================
TASKS.TXT — GEM Feature Backlog
Last updated: 2026-05-19
Format: [x] = done, [ ] = pending, [-] = in progress / partial

Tasks are grouped by the release they target.
Only [x] items that are fully done ship in that version's release notes.1
========================================================

=== v0.8 — Done ===
Released: 2026-05-18

[x] @mention system — parse `@username` in message content on send; resolve to userId;
    emit in-app notification to that user with a link to the message. Frontend: `@`
    autocomplete dropdown with group member search, mention highlighting in rendered
    messages. Backend notification fanout was already wired. DONE 2026-05-14.

[x] Duplicate / copy event — button on EventPage (admin/creator only) that pre-fills
    CreateEventPage with all fields from the current event, letting the user adjust date
    and save as new

[x] Event reminders — scheduleEventStartNotification is wired and queued but never called
    on event create/update; hook it in so RSVP-yes members get a push/email N minutes
    before start (configurable per-user: 15 min, 1 hr, 1 day)
    DONE 2026-05-18: scheduleEventStartNotification wired on event create/update; queues 3 BullMQ
    jobs per event (15m/1hr/1day); worker filters by per-user offset pref; NotificationSettingsPage
    has "Event Reminders" chip UI per channel; reminderOffsetMinutes field added to schema + migrated.
    Files: server.ts, schema.prisma, migrations/20260518*, useNotifications.ts, NotificationSettingsPage.tsx

[x] Empty-state illustrations — replace plain text "No events yet" / "No media" placeholders
    with simple SVG spot illustrations
    DONE 2026-05-18: Added inline SVG spot illustrations to all 5 empty states: calendar icon for
    "No events yet" (GroupPage), landscape photo icon for "No photos yet" (GroupGalleryPage),
    "No media yet" (EventPage, GroupManagePage admin), and people icon for "No groups yet"
    (GroupsPage). EventPage gained EmptyState import. Files: GroupPage.tsx, GroupGalleryPage.tsx,
    EventPage.tsx, GroupManagePage.tsx, GroupsPage.tsx

[x] pick and update favicon for all pages (browser tab, PWA icon, OG image)

[x] fix media upload with storage limits per user (per-user quota enforcement)

[x] fix sizing of event creation page on mobile


[x] Group stats page — GET /groups/:groupId/stats, GroupStatsPage.tsx, linked from
    GroupManagePage. Shows total events, RSVPs, messages, photos, storage, top members,
    top tags. Admin-only. DONE 2026-05-14.

[x] Updates page (was: Changelog) — full version history at /updates, accessible from
    sidebar and public site. API health check button inline. DONE 2026-05-14.

[x] Scroll fix on marketing pages — MarketingLayout outer div changed to h-full with
    overflow-y-auto on main, fixing the iOS keyboard lock conflict. DONE 2026-05-14.

[x] Admin photo delete in group media lightbox — trash button in carousel top bar (next to
    info button) with two-step inline confirm. Calls DELETE /groups/:groupId/media/:assetId.
    Available to owners/admins only; works in both All Photos and album views. DONE 2026-05-14.

[x] Character limits across all major input fields — event title (100), event details (3000),
    event location (200), group name (60), group description (500), channel message (2000),
    channel name (32), photo caption (280). Enforced in Zod schemas (API) and maxLength
    attrs (frontend). DONE 2026-05-14.

[x] Leave group — Members tab in GroupPage now has "Leave Group" button with confirm step for
    non-owner members. API: DELETE /groups/:groupId/leave (owner blocked). Navigates to /groups
    on success. DONE 2026-05-19. Files: server.ts, GroupPage.tsx, useGroups.ts

[x] Stats page toggle — Admin toggle in GroupManagePage (Group Info section) enables/disables
    stats page for all group members. When enabled: Stats link shows in GroupPage header for all
    members; GET /groups/:groupId/stats allows all members (not just admin). Schema: Group.statsEnabled.
    DONE 2026-05-19. Files: schema.prisma, server.ts, GroupPage.tsx, GroupManagePage.tsx, useGroups.ts

[x] Stats link on user profile — Mutual groups with statsEnabled show a "Stats" link button next
    to the group card on UserProfilePage (/u/:username). API returns statsEnabled on mutual groups.
    DONE 2026-05-19. Files: server.ts, UserProfilePage.tsx

[x] Email visibility toggle — ProfilePage settings now have a toggle "Show email on profile".
    When on, other group members can see the user's email on their public profile page. Schema:
    User.showEmail. DONE 2026-05-19. Files: schema.prisma, server.ts, ProfilePage.tsx,
    UserProfilePage.tsx, authStore.ts

=== v0.9 — Polish & Stability ===
Target: tighten the core experience before semi-public release; no new major features,
focus on reliability, UX rough edges, and prep for a small group of external users.

--- UX polish ---

[ ] Fix window sizing on desktop — inconsistent div sizing on desktop layout causes layout
    overflow in some page combinations; audit all page wrappers

[ ] When keyboard opens on mobile channel view — shrink header and slide it toward the top
    border so the message input doesn't get pushed off screen


[ ] Keyboard shortcut help modal — ? key opens an overlay listing all keyboard shortcuts
    (lightbox arrow nav, Escape, submit, etc.)

[ ] Offline queue for RSVPs — when user RSVPs while offline, queue action in localStorage
    and replay on reconnect (extends existing useIsOnline hook)

--- Notifications & email ---

[ ] Digest emails — daily or weekly email summarising upcoming events the user is
    RSVP-yes on; scheduled via BullMQ repeatable job; opt-in toggle in NotificationSettingsPage

[ ] In-app notification grouping — collapse consecutive notifications of the same type for
    the same event (e.g. "5 people RSVPed to your event") into a single row

[x] Specify sender in push/email notifications — include the acting user's display name
    and avatar URL in fanout payload so notifications read "Alice RSVPed…" not just "New RSVP"
    DONE 2026-05-20: chat_message title now "{sender} in #{channel}" (added name to chat.ts
    callback payload); invite title/body now "${actor} invited you" / "${event.title}"; other
    notification types already included actor names. EventPage edit form: date+duration grid
    changed from grid-cols-2 to grid-cols-1 sm:grid-cols-2 so duration wraps to its own row
    on mobile. Files: lib/chat.ts, server.ts, EventPage.tsx

--- Events ---

[ ] Waitlist — when maxAttendees is reached, late RSVPs join a waitlist; promote
    automatically when a yes-RSVP withdraws (add status: "waitlist" on RSVP model)


[ ] Event page map embed — when event.location is set, render an embedded static map
    thumbnail (OpenStreetMap tile, no API key needed) above the directions link

[x] Auto-hyperlinking in channel chat — URLs typed in messages automatically become clickable
    links opening in a new tab. Preserves @mention detection. DONE 2026-05-19. Files: ChannelPage.tsx

[x] Join modal Code/URL tabs — "Join a Group" modal now has Code tab (invite code input) and
    URL tab (paste full invite link with clipboard paste button). DONE 2026-05-19. Files: GroupsPage.tsx

[x] Inline user stats on profile — replaced the "Stats" button on mutual-group cards (which
    linked to the group-wide stats page) with per-group inline stats showing events created,
    going/maybe RSVPs, and photos uploaded directly below each group card on the user profile
    page. New API endpoint GET /groups/:groupId/members/:userId/stats.
    DONE 2026-05-20. Files: server.ts, UserProfilePage.tsx, notes.md

--- Infrastructure ---

[ ] Error monitoring — integrate a lightweight error tracker (e.g. Sentry free tier) for
    both API (Fastify plugin) and web (ErrorBoundary + Sentry.captureException)

[ ] Automated DB backups — nightly pg_dump cron job to a separate S3/R2 bucket;
    retention 7 days; alert on failure--how can we do this for free? how does it interact with api? different server?

=== v1.0 — Semi-Public Beta ===
Target: controlled expansion beyond the core dev team to a handful of trusted external
users (~2–5 people). Not publicly advertised. Goal: validate that the app works reliably
for people who weren't involved in building it — real onboarding, real feedback.

Audience: developers + 1–2 invited external users (friends/family) who get a beta invite code.

--- Onboarding & first-run ---

[x] First-run onboarding flow — after registration, show a 3-step tour: create a group,
    invite a friend, create your first event; dismissable; stored in User.onboardingDone
    DONE 2026-05-19: Modal shown on GroupsPage when user has 0 groups and onboardingDone=false;
    dismiss calls PATCH /users/me { onboardingDone: true }. Schema: User.onboardingDone.
    Files: schema.prisma, server.ts, GroupsPage.tsx, authStore.ts

[x] Welcome email — send a formatted welcome email on account creation (uses existing
    SMTP pipeline); includes link to Help page and contact address
    DONE 2026-05-19: Sent after email verification (both OTP and link paths) via sendTransactionalEmail.
    Only fires when isMailConfigured(). Files: server.ts

[x] Better empty state on GroupsPage for brand-new users — prompt to create first group
    with a clear CTA button when the user has no groups
    DONE 2026-05-19: 3-step illustrated welcome layout with icon, heading, numbered steps,
    and Join/Create buttons when data.groups is empty. Files: GroupsPage.tsx

--- Reliability & quality gate ---

[ ] End-to-end smoke test suite — Playwright tests covering: register → join group →
    create event → RSVP → channel message → push notification; run before each release

[ ] Load test baseline — confirm the API handles 50 concurrent users without degradation
    on the current VPS (k6 or autocannon); document results in notes.md

[ ] Graceful error pages — replace raw JSON error responses visible in the UI with
    friendly error cards (already have 404; add 403, 500 variants)

--- Invite & access control ---

[ ] Invite-code dashboard — admin UI showing all issued beta codes (account + group
    creation), who used them, and when; currently only code issuance exists in DevPanel

[ ] Rate-limit invite code generation — max 10 active account codes at a time; prevent
    accidental open registration

[ ] Account deletion — GDPR-style self-serve delete in Settings; removes all personal data
    (profile, messages anonymised, photos deleted from storage); 30-day soft delete before purge

--- Polish ---

[ ] Favicon and OG image audit — confirm og:image, og:title, og:description are set
    correctly on all public pages (Landing, Help, Contact, Updates)

[ ] Accessibility audit — run axe-core against all public pages and the main app shell;
    fix any critical/serious violations before v1.0 tag

[ ] PWA update prompt — when a new service worker is waiting, show a banner asking the
    user to refresh; currently the update is silent and may confuse new users

[ ] Legal / privacy basics — add a minimal Privacy Policy page at /privacy explaining
    what data is stored (email, photos, messages) and how it is used; linked from footer

=== Later / Stretch ===

[ ] recurring events — RRULE support; needs recurrenceRule on Event + RRULE UI

[ ] specify which user sent message in push/email notifications

[ ] text sizing / accessibility settings per user

[ ] login links security hardening (CSRF, same-site cookies review)

[ ] waitlist to make account (public landing page signup form)

[ ] demo mode — not logged in, temporary session for first-time visitors

[ ] add 'social' functionality — friends via invite link, global username search,
    mutual groups

[ ] verify account via alternative channel (WhatsApp, Telegram, SMS)

[ ] Google Calendar note — URL subscription shows feed as "Other calendars" sidebar
    in Google Calendar (by-design behavior for all external URL subscriptions).
    Consider adding a UI note so users understand this is expected.

=== Done (historical, merged from previous backlog) ===

[x] avatar proxy endpoint — DONE 2026-04-27: Added GET /media/proxy/* route in server.ts
    (avatars-only restriction, 1-yr cache, streams S3 body); added S3_PUBLIC_BASE_URL.

[x] restart API service to pick up per-user mute routes — DONE 2026-04-27

[x] group channels — DONE 2026-05-06: General channel auto-created on group creation.
    Channel tabs, subscriptions, real-time chat via Socket.IO.

[x] Phase 7 manual device matrix — DONE: iOS Safari, Android Chrome, desktop installability,
    push notifications, and calendar clients validated on real devices.

[x] add google maps API key + address autocompletion in event creation page — DONE 2026-05-06

[x] when click calendar button in events, switch to pop-up modal — DONE 2026-05-06

[x] add view controls on event page (grid, list) — DONE

[x] notifications pop-up modal: no scroll wheel — DONE

[x] update all email notifications to have accurate links — DONE

[x] add changing member roles (demote/promote) to audit log — DONE

[x] add enable push notifications pop-up if notis are not enabled — DONE

[x] get rid of event chat — DONE

[x] fully implement notifications page — DONE

[x] add PWA install guide — DONE

[x] on private events, mark event as private with a lock glyph — DONE

[x] make sure no one gets notified for anything tied to past events — DONE

[x] on private events, display list of users who were invited — DONE

[x] add get notified feature on event start for RSVP yes events — DONE

[x] account creation code invite link in developer panel — DONE

[x] denote admins with badge glyph in members page — DONE

[x] ICS download with auth Bearer token — DONE 2026-04-26

[x] make live calendar subscription via secret token — DONE 2026-04-26

[x] RSVP hover tooltip showing status — DONE 2026-04-26

[x] Good morning/evening greeting on groups page — DONE 2026-04-26

[x] security hardening pass — DONE 2026-04-28: CSP, CORS, metrics auth, JWT expiry,
    in-memory token, crypto-random beta codes, etc.

[x] 404 error page — DONE 2026-04-28

[x] fix logout on hard refresh — DONE 2026-04-28: switched to sessionStorage

[x] fix push notifications honoring settings page toggles — DONE 2026-04-29

[x] move notification bell to top-right page banner — DONE 2026-04-29

[x] fix PWA relaunch logout + extend session duration — DONE 2026-04-29

[x] group invite link breaks the session (new tab) — DONE 2026-04-29: switched to localStorage

[x] if make invite-only channel, auto-join the creator — DONE

[x] invite-only channel: show lock glyph — DONE

[x] reply to a message in channel chat — DONE (inline quoted preview)

[x] on public pages, top right hover dropdown hangs 200ms — DONE

[x] Audit log CSV export — DONE

[x] EXIF metadata on uploaded photos — DONE 2026-05-14: camera, lens, focal length,
    aperture, shutter, ISO, GPS, dimensions via exifr + image-size

[x] Group Photos tab — DONE 2026-05-14: grid of all group photos, newest-first, opens
    MediaLightbox with full EXIF panel

[x] GEM favicon in desktop sidebar — DONE 2026-05-14

[x] Profile picture in sidebar clickable to profile page on desktop — DONE 2026-05-14
